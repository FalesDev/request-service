server:
  port: 8090
spring:
  application:
    name: "request-service"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
logging:
  level:
    co.com.pragma: trace
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"

services:
  auth:
    url: "http://localhost:8080"

adapters:
  r2dbc:
    host: "localhost"
    port: 5433
    database: "request"
    schema: "public"
    username: "admin"
    password: "admin"

jwt:
  secret: rXm5xuejgjmoMoOaaP5G5EUl6Wl+nZxjHLMx2n8IPHE=